# 🚀 Быстрый старт с iPhone

## Пошаговая инструкция

### 1️⃣ Настройте iPhone Personal Hotspot

На вашем iPhone:

- **Настройки** → **Режим модема** (Personal Hotspot)
- Включите переключатель **"Разрешать другим"**
- Запомните:
  - **Имя сети** (обычно "iPhone ИМЯ")
  - **Пароль WiFi**

### 2️⃣ Измените WiFi данные в коде

Откройте `src/main.cpp` и измените строки 10-11:

```cpp
const char *ssid = "DIGIFIBRA-6GDf";        // ← Ваше имя hotspot
const char *password = "XYbSCxGZsK";       // ← Ваш пароль hotspot
```

### 3️⃣ Загрузите код на ESP32

```bash
# Подключите ESP32 к Mac через USB
pio run --target upload
```

### 4️⃣ Узнайте IP-адрес ESP32

```bash
# Откройте Serial Monitor
pio device monitor
```

Вы увидите что-то вроде:

```
========================================
Connecting to WiFi...
SSID: iPhone Ilia
........
✓ Connected to WiFi!
========================================
Network Information:
  IP Address: 172.20.10.2        ← ЭТО ВАШ IP!
  Gateway:    172.20.10.1
  Signal:     -45 dBm
========================================

🌐 Open in browser: http://172.20.10.2
```

### 5️⃣ Откройте веб-сервер на iPhone

На вашем iPhone:

1. Откройте **Safari**
2. Введите IP-адрес: `http://172.20.10.2`
3. Вы увидите страницу управления LED!

---

## 🎯 Преимущества этого подхода

✅ **Не нужен WiFi роутер** - всё работает через iPhone  
✅ **Полностью мобильно** - берите с собой куда угодно  
✅ **iPhone остается с интернетом** - можете пользоваться другими приложениями  
✅ **Простая настройка** - 5 минут и всё готово

---

## 📱 API команды

### Включить LED и повернуть сервопривод:

```
http://172.20.10.2/on?090
```

- LED включится
- Сервопривод повернется на 90°

### Выключить LED:

```
http://172.20.10.2/of
```

---

## ⚠️ Возможные проблемы

### ESP32 не подключается к WiFi

**Проверьте:**

1. ✓ Personal Hotspot включен на iPhone
2. ✓ SSID и пароль в коде верные (с учетом регистра!)
3. ✓ ESP32 находится рядом с iPhone (< 5 метров)
4. ✓ Попробуйте выключить и включить hotspot на iPhone

### Не открывается веб-страница

**Проверьте:**

1. ✓ iPhone подключен к своему hotspot
2. ✓ Используете правильный IP-адрес из Serial Monitor
3. ✓ Вводите `http://` перед IP (не `https://`)
4. ✓ Нет опечаток в IP-адресе

---

## 🔧 Дополнительно

### Изменить имя хоста (чтобы не запоминать IP)

В `setup()` добавьте после `WiFi.begin()`:

```cpp
WiFi.setHostname("esp32-led");
```

Теперь можно открывать: `http://esp32-led.local` (работает на iOS/macOS)

### Посмотреть подключенные устройства на iPhone

На iPhone нет встроенного способа увидеть все подключенные устройства к hotspot, но ESP32 всегда будет виден в Serial Monitor с его IP-адресом.

---

**Готово! Наслаждайтесь управлением ESP32 с iPhone! 📱✨**
